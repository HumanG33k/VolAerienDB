Pour créer la base de donnée, exécuter le fichier banzai.sql à l'aide de la commande @banzai dans sqlplus.
L'exécutable 'projet' se connecte automatiquement sur le compte sql 'eob3320' et travaille sur la base
de donnée créer précédemment.  
